# Third-Party Notices

This project includes the minimal Shroudtopia SDK headers required to build the
client mod:

- `include/shroudtopia.h`
- `include/memory_utils.h`

Those headers originate from Shroudtopia and are included here so the mod can be
built as a small standalone source tree. Review the upstream Shroudtopia project
for its original license and full loader implementation.
